import { DefaultTheme } from 'react-native-paper';

const theme = {
  ...DefaultTheme,
  colors: {
    ...DefaultTheme.colors,
    primary: '#16ABFD',
    secundary: '#f66',
    accent: 'red',
    colorText: '#fff',
  },
};

export default theme;
