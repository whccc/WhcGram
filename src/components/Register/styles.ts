import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  Container: {
    marginTop: 80,
    width: '100%',
    padding: 10,
  },
  ContainerImg: {
    display: 'flex',
    alignSelf: 'center',
  },
  Img: {
    borderRadius: 100,
    width: 100,
    height: 100,
  },
  Button: {
    marginTop: 5,
  },
});

export default styles;
